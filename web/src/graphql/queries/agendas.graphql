query Agendas($limit: Int!, $cursor: String) {
  agendas(limit: $limit, cursor: $cursor) {
    name
    description
    id
    startTime
    endTime
    organizer {
      username
    }
    venue
  }
}
